# parse-server-template

## Start

1. Add `.env` file in the local directory

```bash
cat .env.example > .env
```

2. Fill values of the file

3. run

```bash
# install packages
yarn install
# then
yarn run start
```